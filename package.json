{
  "name": "learn-while-building",
  "version": "0.5.0",
  "description": "Real-time teaching companion for Claude Code — learn programming while AI builds your project",
  "type": "module",
  "scripts": {
    "build": "tsc && cp -r src/web dist/src/web",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "@fastify/websocket": "^11.2.0",
    "fastify": "^5.8.4",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@sindresorhus/tsconfig": "^8.1.0",
    "@types/ws": "^8.18.1",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
